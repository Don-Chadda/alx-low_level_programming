#include "main.h"

/**
 * _iasuper - checks for upercase character
 * @c: the character to be checked
 * Return: 1 if c is uppercase, 0 otherwise
 */
int _super(int c)
{
	return (c >= 'A' && c <= 'Z');
}
